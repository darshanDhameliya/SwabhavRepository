package com.techlab.board;

import java.util.HashMap;

public class Board {
	private HashMap<String, Cell> board = new HashMap<String,Cell>();

	public Board() {
		board.put("A1", new Cell());
		board.put("A2", new Cell());
		board.put("A3", new Cell());
		board.put("B1", new Cell());
		board.put("B2", new Cell());
		board.put("B3", new Cell());
		board.put("C1", new Cell());
		board.put("C2", new Cell());
		board.put("C3", new Cell());
	}

	public void setBoardCellValue(String key,Mark mark)
	{
//		board.put(key, mark);
	}
	public Mark getBoardCellValue(String key)
	{
//		return board.get(key);
	}
}
