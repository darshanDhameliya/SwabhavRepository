package com.techlab.game.runner;

import java.util.Scanner;

import com.techlab.board.Board;
import com.techlab.board.Cell;
import com.techlab.player.Player;

public class GameRunner {
	private Board board;
	private Cell[] boardCells;
	private int GO_TO_NEXT_ROW=3;
	public GameRunner(Board board)
	{
		this.board=board;
	}
	public void setPlayerSignInBoard(Player player1,Player player2)
	{
	    Scanner in = new Scanner(System.in);
        int cellPosition = in.nextInt();
        System.out.println("You entered string "+cellPosition);
        board.setBoardCellMark(cellPosition, player1.getMark());
	}
	public void displayBoard()
	{
		boardCells=board.getBoardState();
		
		for (int index = 0; index < boardCells.length; index=index+GO_TO_NEXT_ROW) {
			
		}
	}
}
