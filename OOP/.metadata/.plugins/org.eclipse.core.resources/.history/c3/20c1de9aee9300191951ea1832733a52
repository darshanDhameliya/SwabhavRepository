package com.techlab.calculation;

public class Calculation {

	public static int findMaxNumber(Integer arr[]) {
		Integer max=arr[0];
		for (Integer integer : arr) {
			if (max<integer)
				max=integer;
		}
		return max;
	}
}
